- clean: ['~']

- shell:
    -
      command: ./install -c install.conf.ess.yaml
      description: Install essential dotfiles
      stdin: true
      stdout: true
    -
      command: ./install -c install.conf.core.yaml
      description: Install core dotfiles
      stdin: true
      stdout: true

    -
      command: ./install -c install.conf.snap.yaml
      description: Install core dotfiles
      stdin: true
      stdout: true

    -
      command: ./install -c install.conf.apt.yaml
      description: Install core dotfiles
      stdin: true
      stdout: true